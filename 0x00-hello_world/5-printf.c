#include<stdio.h>
/**
 * main - A programe to print exactly the sentence below followed by a new line
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
        printf("with proper grammar, but the outcome is a piece of art, \n");
        return (0);
}
