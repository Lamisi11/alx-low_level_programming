#include "main.h"
/**
* add - add two integers a and i and returns the result
* @a: int to be added to i
* @i: int to be added to a
* Return: sum of a and i
*/
int add(int a, int i)
{
	int r;

	r = a + i;
	return (r);
}
